# 작업 과정

## 2026-09-15: 설계 초안과 작업 정의

- 사용자가 3주차 README를 기준으로 OpenRouter + DeepSeek 4.1 구현을 요청했다.
- 앞선 대화의 관리자 1명·독립 입찰자 3명, 작업마다 새 대화, 실행별 상태 초기화 설계를 적용한다.
- 사용자가 게임형 과제를 원하지 않아 신제품 출시 준비 업무를 합성 데이터로 구성했다.
- Codex가 작업·역할·코드를 작성하며 사용자의 검토와 결과 해석이 남아 있음을 명시한다.
- tasks.json과 gold를 모델 실행 전에 먼저 커밋한다.
- 현재 프로젝트 .env의 키는 존재하지만 OpenRouter 키 형식이 아니어서 다른 서비스로 전송하지 않는다.
- 실제 API 결과와 모의 응답을 구분한다. 테스트 응답으로 results.csv나 실험 로그를 채우지 않는다.
- AGENTS.md의 사용자 PR 확인 규칙은 로컬에 유지한다. 이번 작업에서 PR을 만들지 않는다.

## 첫 구현과 모의 검증

- 역할·입찰 처리, OpenRouter 전송, 실행기·기록기를 각각 별도 커밋했다.
- 외부 패키지 없이 Python 표준 라이브러리로 구현했다.
- 첫 16개 모의 테스트에서 잘못된 gold 타입과 매우 큰 confidence 값이 정상적인 검증 오류 대신 예외를 내는 문제가 발견됐다.
- 실패 출력을 diagnostics/01-unittest-initial.log에 보존하고 테스트와 함께 먼저 커밋했다.
- gold 타입 검사와 confidence 범위 검사 순서를 수정했다. HTTP 오류 응답도 명시적으로 닫아 리소스 경고를 해소했다.

## 검증과 인증 경계

- 수정 후 모의 테스트 16개가 통과했다. Python 3.12.11의 프로젝트 가상환경으로 확인했다.
- plan은 6개 작업, 9회 실행, 정상 162회·재시도 포함 최대 324회 호출을 표시했다.
- 실제 smoke 명령은 키 형식 사전 확인에서 exit 2로 종료했다. 모델 요청·실험 결과는 생성하지 않았다.
- 공식 검사에서 코드·작업표·보고서 형식은 통과했다. 실제 results.csv와 logs/가 없어 2개 검사가 실패했다.
- 공식 검사기의 키 검사가 ignored 파일도 순회함을 확인했다. 실제 키 파일을 week-03 안에 두지 않고 상위 학번 폴더의 .env만 기본값으로 읽도록 수정했다.
- 기존 키 값은 유지하고 상위 .env에 빈 OPENROUTER_API_KEY 입력 항목을 준비한다. 키를 제공받거나 사용한 것으로 기록하지 않는다.

## 실제 연결 점검: 인증과 공급자 정책

- 최초 키는 조회 API에서는 인증됐지만 모델 호출에는 HTTP 401이 발생했다. 이후 메타데이터로 추론 호출을 지원하지 않는 Management Key임을 확인했다. 실패 로그와 진단 응답을 diagnostics/에 보존했다.
- Management Key로 사용 한도 $1인 과제 전용 추론 키 한 개를 생성해 로컬 파일에 저장했다. 그 키의 직접 DeepSeek 공급자 요청은 계정의 데이터 정책 때문에 HTTP 404로 거절됐다. 계정 정책은 변경하지 않았다.
- 이후 사용자가 OPENROUTER_API_KEY를 다시 입력했다. 새 키의 인증 성공과 일반 추론 키 여부를 확인했다. 이 키는 위의 $1 제한 키와 별개이며 메타데이터상 키별 한도가 없다. 키 값은 출력하거나 기록하지 않았다.
- 아직 성공한 본 실험은 없으므로 모델은 그대로 두고 공급자를 DeepInfra FP8로 고정해 연결을 재검증한다. 조건 간에는 공급자도 동일하게 유지한다.
- 모든 인증 실패·정책 실패는 smoke 진단이며 results.csv의 실험 결과에 섞지 않는다.

## 공급자 혼잡과 두 번째 고정 공급자

- 새 사용자 키 + DeepInfra FP8로 smoke의 독립 호출 3개가 모두 성공했다. A가 입찰하고 B·C는 거절해 첫 작업을 A에게 배정했다. diagnostics/smoke-20260915T120934-a2ea9500-baseline.log에 원문을 보존했다.
- 이어진 baseline 두 번은 각각 첫 호출과 재시도에서 HTTP 429로 중단됐다. 응답은 DeepInfra upstream_provider_shared_pool의 engine_overloaded를 명시했다. 두 crashed 행의 수치는 비워 두고 CSV와 logs/에 남겼다.
- DeepInfra 혼잡이 반복되어 동일 모델을 제공하는 Fireworks로 공급자를 고정했다. 모델·작업·온도·역할·토큰·추론·선정 규칙은 그대로다. 공급자 변경으로 experiment_id가 달라지므로 앞선 실패와 조건 비교 결과를 합산하지 않는다.

## 연결 가능한 공급자 확인

- Fireworks의 baseline 호출도 HTTP 429로 실패했다. Relace FP4를 별도 smoke로 확인했지만 같은 upstream 공용 풀 오류가 발생했다. 실패 원문과 crashed 기록을 보존했다.
- 본 실험과 분리한 1회 진단에서 동일 모델·파라미터·단가 상한을 유지하고 공급자 자동 라우팅을 허용했다. Novita에서 정상 JSON 입찰이 반환됐다. 이 응답은 results.csv에 포함하지 않는다.
- 공개 모델 endpoint API에서 Novita의 정확한 태그 novita/fp8과 파라미터 지원을 확인했다. 입력 $0.30/M, 출력 $1.20/M로 기존 단가 상한 이내다.
- 본 실험은 Novita FP8 단일 공급자로 다시 고정하며 fallback=false로 유지한다. 새로운 experiment_id를 사용한다. 계정 데이터 정책은 변경하지 않았다.
