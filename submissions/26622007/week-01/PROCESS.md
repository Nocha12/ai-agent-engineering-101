# 작업 과정

## 2026-09-15: 제출 환경 설정

- 사용자 요청: 강의 사이트와 GitHub에서 과제 제출 방법을 찾아 현재 프로젝트 폴더에 설정. 이후 학번 `26622007` 제공.
- Codex가 수행한 작업: 공식 강의·과제 명세·저장소 규칙 확인, 개인 Fork 및 Git remote 설정, 본인 roster 등록 파일과 PR 준비, Python 3.12 환경과 SDK 설치.
- 시작 코드: `weeks/week-01/starter/first_agent_openai.py`를 `first_agent.py`로, 공식 `notes.txt`를 같은 폴더로 그대로 복사.
- 선택 이유: 현재 환경에 `OPENAI_API_KEY`가 설정되어 있어 공식 OpenAI 호환 시작 코드를 선택함. 키 값은 읽거나 기록하지 않음.
- 환경 확인: SDK import, 시작 코드 문법, SDK 메서드 존재, 설치 패키지 호환성 확인. 모델 API 호출은 수행하지 않음.
- 아직 수행하지 않은 과제 작업: 세 번째 도구 구현, 실제 태스크 실행, 도구 설명 작성, 실행 결과 관찰.
- 폐기한 과제 구현: 없음. 이번 작업은 초기 설정임.

## 이후 기록할 내용

각 작업에서 에이전트에 시킨 일, 시도한 방법, 버린 방법과 이유, 사용한 모델과 실행 명령을 실제 수행한 내용대로 덧붙입니다. 실제 실행 콘솔은 `logs/` 아래 새 파일로 저장합니다.

## 2026-09-15: API 키 설정 방침 변경

- 사용자 지시: 기존 환경의 키에 대해 확인을 요청했고, 사용할 키는 새로 제공하겠다고 알림.
- 확인한 사실: 기존 환경변수의 설정 여부만 확인했으며, 키 값 출력 및 API 호출은 없었음.
- 변경: 새 프로젝트 전용 키 설정 전까지 API 실행 보류. Git에서 제외되는 본인 폴더의 `.env`에 빈 키 항목을 두고, 실행 안내가 이 파일을 명시적으로 불러오도록 수정함.
- 공식 OpenAI 호환 시작 코드는 유지하되, 실제 제공자와 모델은 새 키 설정 시 맞추기로 함.
